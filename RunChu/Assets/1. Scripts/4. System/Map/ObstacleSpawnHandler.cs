using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObstacleSpawnHandler : MonoBehaviour
{
    // private Transform spawnPoint;

    // public void SpawnObstacle(EPoolObjectType obstacleType, Transform parentTransform)
    // {
    //     GameObject obstacle = PoolManager.Instance.SpawnFromPool(obstacleType);
    //     obstacle.transform.position = spawnPoint.position;
    //     obstacle.transform.SetParent(parentTransform, true);
    // }
}
